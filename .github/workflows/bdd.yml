name: BDD Tests

on:
    push:
        branches:
            - main
        paths-ignore:
            - '**.md'
    pull_request:
        branches:
            - main
        paths-ignore:
            - '**.md'

jobs:
    build:
        runs-on: ubuntu-latest
        container: python:3.9-slim

        services:
            postgres:
                image: postgres:alpine
                ports:
                    - 5432:5432
                env:
                    POSTGRES_PASSWORD: postgres
                options: >-
                    --health-cmd pg_isready
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5

        steps:
            - name: Checkout
              uses: actions/checkout@v2

            - name: Install dependencies
              run: |
                  apt-get update
                  apt-get install -y gcc libpq-dev
                  apt-get install -y curl wget python3-selenium chromium-driver

            - name: Install packages
              run: |
                  python -m pip install --upgrade pip wheel
                  pip install -r requirements.txt

            # - name: Run the server locally
            #   run: |
            #       echo "\n*** CREATING TEST DB ***\n"
            #       curl -X PUT http://admin:pass@couchdb:5984/test
            #       echo "\n*** STARTING APPLICATION ***\n"
            #       gunicorn --log-level=critical --bind=0.0.0.0:8080 service:app &
            #       sleep 5
            #       curl -I http://localhost:8080/
            #       echo "\n*** SERVER IS RUNNING ***"
            #   env:
            #       BINDING_CLOUDANT: '{"username":"admin","password":"pass","host":"couchdb","port":5984,"url":"http://admin:pass@couchdb:5984"}'

            - name: Run the server locally
              run: |
                gunicorn --log-file=- --workers=1 --bind=0.0.0.0:8080 --log-level=info service:app & 
                sleep 5
                curl -I http://localhost:8080/
                echo "\n*** SERVER IS RUNNING ***"
              env:
                POSTGRES_HOST: postgres
                POSTGRES_PORT: 5432
                FLASK_APP: service:app
                FLASK_ENV: development
                DATABASE_URI: postgresql://postgres:postgres@postgres:5432/postgres

            - name: Run unit tests with behave
              run: behave
